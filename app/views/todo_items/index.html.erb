<p id="notice"><%= notice %></p>

<h1 id="list">Todo Items</h1>

<table>
  <thead>
    <tr>
      <th id="description">Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @todo_items.each do |todo_item| %>
      <tr>
        <td><%= todo_item.description %></td>
        <td id="show"><%= link_to 'Show', todo_item %></td>
        <td id="edit"><%= link_to 'Edit', edit_todo_item_path(todo_item) %></td>
        <td id="destroy"><%= link_to 'Destroy', todo_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Todo Item', new_todo_item_path %>
